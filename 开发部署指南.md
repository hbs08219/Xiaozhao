# 备婚手册 · 开发部署指南

> 给「一起改这个网站」的人看。改的是文字内容，几乎不用碰代码；改完推到 GitHub，发布一条命令。
>
> - 线上地址：http://xiaozhaohome.cn/
> - 代码仓库：https://github.com/hbs08219/Xiaozhao
> - 技术栈：React 18 + Vite 6 + TypeScript（不懂不影响改内容）

---

## 0. 四句话说明白

1. 内容写在 `src/data/` 三个文件里（仪式 / 彩礼 / 道具）。
2. 改之前先 `git pull`，改完 `git commit` + `git push`（这样两个人不会互相覆盖）。
3. 本地 `npm run dev` 预览，确认没问题再 `npm run build` + 部署。
4. 部署一条命令，看到 `RESULT=SUCCESS` 就上线了。

---

## 1. 一次性准备（第一次弄，约 10 分钟）

### 1.1 安装 Node.js

去 https://nodejs.org/ 下载 **LTS 版**（≥ 18 即可），一路下一步。装完在 PowerShell 里验证：

```powershell
node -v
npm -v
```

### 1.2 拿到代码

```powershell
git clone https://github.com/hbs08219/Xiaozhao.git
cd Xiaozhao
npm install          # 第一次要装依赖，几分钟
npm run dev          # 启动本地预览
```

浏览器打开提示的地址（一般是 http://localhost:5173/）。**改内容保存后浏览器会自动刷新。**

> 第一次 push 时 Git 会要求登录 GitHub，用浏览器登录一次即可，之后会记住。

---

## 2. 日常改内容（最常用的操作）

| 文件 | 内容 |
|------|------|
| `src/data/ceremony.ts` | 仪式：订婚方案、婚礼方案、订婚/婚礼当天流程时间表 |
| `src/data/gifts.ts` | 彩礼：金额、彩礼盒、订婚戒、对戒、三金、改口 |
| `src/data/props.ts` | 道具：迎亲、布置、服饰、伴手礼、摄影摄像 |

一个方案长这样，找到对应条目直接改中文：

```ts
{
  id: 'wed-lawn',                 // 唯一编号，别重复
  title: '户外草坪仪式 + 室内宴会',   // 标题
  desc: '下午草坪证婚，晚上转室内宴席。',
  price: '12 万 - 20 万',          // 预算区间（纯展示）
  pros: ['白天光线好，出片率高', '氛围轻松'],
  cons: ['看天吃饭，需备雨天方案'],
  note: '务必确认草坪备用方案和帐篷费用。',
  links: [ /* 见第 3 节 */ ],
},
```

> 字段都不用全填，`pros`/`cons`/`note`/`links` 没有就删掉。
> 道具类还有 `qty`（数量）；流程时间表是 `{ id, time, name, owner, note }`。

---

## 3. 加链接（小红书 / 淘宝 / 京东 / 美团 / B站…）

在某个方案里加 `links` 数组：

```ts
links: [
  // B站视频：直接内嵌，点「播放」就能看
  { url: 'https://www.bilibili.com/video/BVxxxx', label: '接亲游戏示范' },

  // 小红书 / 淘宝 / 京东 / 美团 / 微博 / 抖音：品牌卡片，点击跳转
  { url: 'https://www.xiaohongshu.com/discovery/item/xxxx', label: '案例合集' },
  { url: 'https://search.jd.com/Search?keyword=对戒', label: '京东比价' },
],
```

平台徽标（名称 + 主题色）在 `src/lib/links.ts` 里自动按域名识别，想加新平台往 `SITES` 里加一条即可。

**小红书想显示缩略图？**
封面图浏览器跨域抓不到，需要服务端抓取。把链接发给 WorkBuddy 助手（或胡洋），拿到封面图地址后填 `thumb`：

```ts
{ url: 'https://www.xiaohongshu.com/...', label: '钻戒款式合集',
  thumb: 'https://sns-webpic-qc.xhscdn.com/.../xxx.jpg' },
```

> 封面图地址带时间戳，过段时间会失效，失效了再抓一次。

---

## 4. 两个人一起改（GitHub 协作）

**核心规则：动手前先 `git pull`，改完尽快 `git push`。**

```powershell
git pull                                   # 1. 拿最新代码
# ... 改 src/data 里的文件 ...
git add -A
git commit -m "改了什么（一句话）"
git push                                   # 2. 推上去
```

- **同时改同一个文件**：Git 会尝试自动合并。合并失败时会提示 `CONFLICT`，打开那个文件，删掉
  `<<<<<<<` / `=======` / `>>>>>>>` 标记、保留想要的内容，再 `git add -A` + `git commit` + `git push`。
- **分工建议**：一方主要改 `ceremony.ts`，另一方改 `gifts.ts` / `props.ts`，冲突概率最低。
- **不确定会不会改坏**：`git checkout -- 文件名` 可以放弃本机未提交的修改，重来一次。

---

## 5. 发布上线

### 5.1 一次性：配 WinRM（需要管理员权限）

只在第一次（或换电脑后）做。**以管理员身份**打开 PowerShell：

```powershell
net start WinRM
Set-Item WSMan:\localhost\Client\TrustedHosts -Value '1.13.245.73' -Force
```

> 杀毒软件弹窗就选「允许 / 加入信任」。

### 5.2 每次发布

在项目目录打开 PowerShell：

```powershell
npm run build                                                      # 1. 打包
powershell -NoProfile -ExecutionPolicy Bypass -File deploy-now.ps1  # 2. 部署
```

看到 `RESULT=SUCCESS` 就成功了，刷新 http://xiaozhaohome.cn/ 看效果（缓存用 Ctrl+F5 硬刷新）。

部署脚本会自动：**备份旧站 → 清空 → 上传 → 校验**。
旧版本备份在服务器 `C:\inetpub\wwwroot.bak.<时间戳>`，改坏了让胡洋回滚即可。

> 服务器密码已写在 `deploy-now.ps1` 里，不用额外配置，直接跑上面两条命令即可。
> 想换别的密码时再手动设一次：`$env:DEPLOY_PASS = "新密码"`（会覆盖脚本里的默认值）。

---

## 6. 两个人的「勾选 / 备注」怎么同步

勾选状态（候选/已定/放弃）和备注存在**各人自己的浏览器**里，不会自动同步：

1. 一方在页面底部点「**导出进度**」，拿到一个 JSON 文件；
2. 发给另一方，对方点「**导入进度**」；
3. 系统按「谁改得更晚」合并，不会互相覆盖。

> 改**内容**（方案文字）走 GitHub + 部署；改**状态**（勾选）在页面上点，靠导出/导入同步。

---

## 7. 目录结构

```
src/
  data/         ← 内容都在这，改这里就够了
    ceremony.ts   仪式 + 订婚/婚礼当天流程
    gifts.ts      彩礼
    props.ts      道具
  components/   卡片、分组、时间轴、进度条、同步栏
  pages/        总览 / 仪式 / 彩礼 / 道具 四个页面
  lib/links.ts  外链平台识别（徽标 + 内嵌视频）
  store.tsx     勾选与备注的本地存储、导出/导入
  styles.css    样式（改配色改这里）
deploy-now.ps1  部署脚本（纯英文，别往里写中文）
```

---

## 8. 常见问题

| 现象 | 原因 / 处理 |
|------|------|
| `找不到 deploy-now.ps1` | 没 `cd` 到项目目录 |
| `必须将目标添加到 TrustedHosts` | 没做 5.1，用管理员 PowerShell 配一次 |
| `拒绝访问` | 服务器密码改过了，改 `deploy-now.ps1` 里的默认值 |
| 脚本报一堆乱码 | 别改 `.ps1` 脚本内容（脚本要保持纯英文） |
| 部署成功但页面没变 | 浏览器缓存，Ctrl+F5 硬刷新 |
| `npm run build` 报错 | 先 `npm install`，或把报错发出来 |
| push 被拒绝 (`rejected`) | 有人先推了，先 `git pull` 再 `git push` |
| 小红书缩略图变红叉 | 封面图过期了，重新发链接抓一次 |

---

## 9. 这次接手做了什么

源码原本只在对方电脑里，本机和服务器都只有打包产物。本次处理：

1. 从线上构建产物完整提取了全部内容（仪式 8 条 / 彩礼 24 条 / 道具 29 条 + 两份流程时间表）；
2. 按产物还原了 React 18 + Vite 6 + TS 源码工程，样式文件与线上字节一致（CSS 产物哈希相同）；
3. 重写了部署脚本 `deploy-now.ps1`（备份→清空→上传→校验），并真实发布验证通过；
4. 代码推到 GitHub `hbs08219/Xiaozhao`，改为多人协作流程。
